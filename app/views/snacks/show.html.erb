<h1><%=@snack.name%><h1>
<p>Price: $<%=@snack.price%></p>
<p>Locations:</p>
<ul>
  <% @snack.machines.each do |machine|%>
    <li><%=machine.location%> (<%= machine.snack_count%> kinds of snacks, average price of <%=machine.average_snack_price%>)</li>
  <%end%>
</ul>
